# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: frontend-changed
'on':
  push:
    branches:
      - master
    paths:
      - 'frontend/**'
jobs:
  inform_netlify_to_deploy:
    steps:
      - uses: actions/checkout@v2
      - run: curl -X POST -d {} https://api.netlify.com/build_hooks/${{ secrets.NETLIFY_BUILD_HOOK_KEY }}
